#include <stdio.h>
#include <stdlib.h>

int main () {
	int x, y, z, w, op, op1;
	printf ("\nDigite a hora atual da entrada do veiculo\n");
	scanf ("%d", &x);
	printf ("\nDigite os minutos da entrada do veiculo:\n";)
	scanf ("%d", &y);
	printf ("\nDigite a hora de saida do veiculo\n");
	scanf ("%d", &z);
	printf ("\nDigite os minutos de saida do veiculo\n");
	scanf ("%d", &w);
	op = x - z;
	op1 = y - w;
	o
	if (op <= 2) 
		if (op1 <= 60)
			printf ("O valor a pagar eh: 1 reais\n");
	if (op <= 4)
		if (op1 <= 60)
			printf ("O valor a pagar eh: 1,40 reais\n");
	if (op <= 5)
		if (op1 <= 60)
			printf ("O valor a pagar eh: 2 reais\n");
	if (op <= 7)
		if (op1 <= 60)
			printf ("O valor a pagar eh: 3 reais\n");
	if (op <= 9)
		if (op1 <= 60)
			printf ("O valor a pagar eh de 3,40 reais\n");
	if (op <= 10)
		if (op1 <= 60)
			printf ("O valor a pagar eh de 4 reais\n");
	if (op <= 12)
		if (op1 <= 60)
			printf ("O valor a pagar eh de 4,40 reais\n");
	if (op <= 14)
		if (op1 <= 60)
			printf ("O valor a pagar eh de 5 reais\n")
	if (op <= 15)
		if (op1 <= 60)
			printf ("O valor a pagar eh de 6 reais\n");
	if ()
	return 0;
}