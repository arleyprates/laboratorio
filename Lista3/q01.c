#include <stdio.h>
#include <stdlib.h>

int main () {
	int x, max, min;
	printf ("\nDigite numeros inteiros positivos e um negativo para sair\n");
	while (x > 0)
		scanf ("%d", &x);
		max = x;
		if (x )
			
	return 0;
}